'''
remainder function

1 // 2 = 0
-1 // -2 = 0
-1 // 2 = -1
1 // -2 = -1

a % b = a - (a // b) * b

xor = false if both true/both false, otherwise false

eval converts a string to a number
int only when it says integer

checking 4 digist number

while(num >= 1000 and num <=9999):
	num = eval('')

use input function only when it asks for USER input

print(ord('t'))
chhr(ord('C')+7) = 'J'

random.randrange(1,5, stepsize) includes end points 1 not 5
random.randint(1, 5) includes end points 1 and 5

import random
random.random() - random float from [0, 1)

sum_t = 0
for i in range(1,9):
	sum_t += 1/(2**i)
print(sum_t)
'''

